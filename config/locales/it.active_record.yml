it:
  activerecord:
    errors:
      # The values :model, :attribute and :value are always available for interpolation
      # The value :count is available when applicable. Can be used for pluralization.
      messages:
        inclusion: "non incluso nella lista"
        exclusion: "è riservato"
        invalid: "non è valido"
        confirmation: "non coincide la confermazione"
        accepted: "va accettato"
        empty: "non deve essere vuoto"
        blank: "non deve essere vuoto"
        too_long: "è troppo lungo (massimo di {{count}} caratteri)"
        too_short: "è troppo corto (minimo di {{count}} caratteri)"
        wrong_length: "ha una lunghezza sbagliata (dovrebbe essere {{count}} caratteri)"
        taken: "è già stata presa"
        not_a_number: "non è un numero"
        greater_than: "deve essere più grande di {{count}}"
        greater_than_or_equal_to: "deve essere più grande o uguale a {{count}}"
        equal_to: "deve essere uguale a {{count}}"
        less_than: "deve essere più piccolo di {{count}}"
        less_than_or_equal_to: "deve essere più piccolo o uguale a {{count}}"
        odd: "deve essere dispari"
        even: "deve essere pari"
        record_invalid: "Convalida fallita: {{errors}}"
        # Append your own errors here or at the model/attributes scope.

      full_messages:
        format: "{{attribute}} {{message}}"

      # You can define own errors for models or model attributes.
      # The values :model, :attribute and :value are always available for interpolation.
      #
      # For example,
      #   models:
      #     user:
      #       blank: "This is a custom blank message for {{model}}: {{attribute}}"
      #       attributes:
      #         login:
      #           blank: "This is a custom blank message for User login"
      # Will define custom blank validation message for User model and 
      # custom blank validation message for login attribute of User model.
      #models:
        
    # Translate model names. Used in Model.human_name().
    #models:
      # For example,
      #   user: "Dude"
      # will translate User model name to "Dude"
    
    # Translate model attribute names. Used in Model.human_attribute_name(attribute).
    #attributes:
      # For example,
      #   user:
      #     login: "Handle"
      # will translate User attribute "login" as "Handle"

