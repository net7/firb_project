<%= render :partial => '/boxview/shared/widget_trascrizione_fenomeni_note', 
		:locals => {:resource => @resource, :fenomeni => nil, :notes => nil, :content => @content, :widget_title => "Trascrizione" } 
%>

<%=
	boxview_widget 'Studio', {:class => 'studio widget_draggable'} do |w| 
                w.content boxview_widget_field('Commento', @resource.comment) if @resource.comment.present?

	end
%>
